{
  "name": "labor-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Lightweight labor management portal (static, localStorage-backed)",
  "scripts": {
    "start": "python3 -m http.server 8000 -d public"
  }
}

{
  "name": "labor-portal",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
